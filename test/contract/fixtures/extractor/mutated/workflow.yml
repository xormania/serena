name: tests
on: [pull_request]
env:
  MARKERS_JVM_RENAMED: java or kotlin
  MARKERS_NATIVE: cpp or rust
  MARKERS_OTHER_LANGS: qml or cue
  MARKERS_NICHE: msl
jobs:
  cpu:
    timeout-minutes: 60
    strategy:
      matrix:
        os: [ubuntu-latest]
        batch: [jvm, native, other-langs, niche, catch-all]
    steps: []
